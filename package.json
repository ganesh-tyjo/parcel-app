{
  "name": "parcel-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run clean && parcel src/index.html --out-dir dist --public-url ./",
    "prod": "npm run clean && parcel build src/index.html --out-dir dist --public-url ./",
    "clean": "rimraf dist && rimraf .cache"
  },
  "keywords": [],
  "author": "Ganesh Shinde",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.14.6",
    "@babel/plugin-transform-runtime": "^7.14.5",
    "@babel/runtime-corejs2": "^7.14.6",
    "autoprefixer": "^9.8.6",
    "parcel-bundler": "^1.12.5",
    "rimraf": "^3.0.2",
    "sass": "^1.34.0"
  }
}
